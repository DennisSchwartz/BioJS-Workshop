// if you don't specify a html file, the sniper will generate a div with id "rootDiv"

var app = require("biojs-vis-gcpiechart");


var instance = new app({
    el: rootDiv,
    sequence: "TGACGATGATCAGAT"
});
